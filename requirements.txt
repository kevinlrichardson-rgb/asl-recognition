# Core ML / Vision — CPU build (no CUDA required)
# For Jetson/CUDA setup, see requirements-jetson.txt
torch>=2.1
numpy>=1.26
opencv-python-headless>=4.8    # headless is fine: infer.py has --headless, app.py uses Gradio
mediapipe>=0.10.8

# Web UI
gradio>=5.0

# Training utilities
scikit-learn>=1.3
tqdm>=4.66
pandas>=2.1

# Evaluation / NLP
pyspellchecker
